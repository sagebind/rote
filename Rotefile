cargo = require("cargo")
default "debug"

task("debug", {}, function()
    cargo.build()
end)

task("release", {}, function()
    cargo.build {
        release = true
    }
end)

task("clean", {}, function()
    cargo.clean()
end)
